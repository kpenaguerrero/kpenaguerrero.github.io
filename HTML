<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Kaylee Peña Guerrero</title>

  <style>
    /* ---------- BRAND COLORS ---------- */
    :root {
      --blush: #E7A8A1;
      --peach: #F2C9B8;
      --cream: #F7F1E8;
      --terracotta: #CB6E55;
      --brown: #7A4E3A;
    }

    /* ---------- GLOBAL STYLES ---------- */
    body {
      margin: 0;
      padding: 0;
      font-family: "Georgia", serif;
      background-color: var(--cream);
      color: var(--brown);
      line-height: 1.6;
    }

    .container {
      max-width: 800px;
      margin: 0 auto;
      padding: 3rem 1.5rem;
    }

    h1 {
      font-size: 3rem;
      margin-bottom: 0.5rem;
      color: var(--terracotta);
      font-weight: 700;
    }

    h2 {
      font-size: 1.4rem;
      margin-top: 0;
      color: var(--brown);
      font-style: italic;
    }

    p {
      font-size: 1.1rem;
      margin-bottom: 1.5rem;
    }

    /* ---------- BUTTONS ---------- */
    .button {
      display: inline-block;
      padding: 0.75rem 1.5rem;
      margin: 
